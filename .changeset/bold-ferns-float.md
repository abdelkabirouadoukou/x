---
"@thexjs/core": patch
"@thexjs/cli": patch
"@thexjs/env": patch
"@thexjs/adapter-vercel": patch
---

Add runtime test coverage for the request pipeline, server actions, image
proxy, env validation, Vercel build output, and the CLI; add release
automation (changesets + CI enforcement).
