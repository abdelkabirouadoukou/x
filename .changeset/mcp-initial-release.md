---
"@thexjs/mcp": minor
---

Initial release of the x MCP server. Exposes grounded x framework docs to AI coding agents over stdio (`list_topics`, `get_docs`, `search_docs`, `scaffold_file`), plus a `@thexjs/mcp/docs` subpath export so tooling (llms.txt generators, docs sites) can reuse the same doc content.
