---
"create-thexjs-app": patch
---

Every scaffolded project now ships AI-agent wiring: `.mcp.json` (Claude Code) and `.cursor/mcp.json` pointing at the `thexjs` MCP server, plus an `AGENTS.md` conventions reference with a `CLAUDE.md` pointer.
